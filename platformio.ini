#
# Marlin Firmware — Sermoon D1
# PlatformIO Configuration File
#
# Target: Creality V4.3.1 (STM32F103RET6, ARM Cortex-M3)
# Build: pio run -e creality
# Upload: pio run -e creality -t upload (J-Link)
#

[platformio]
src_dir      = Marlin
boards_dir   = buildroot/share/PlatformIO/boards
default_envs = creality

[common]
default_build_src_filter = +<src/*> -<src/config> -<src/HAL> +<src/HAL/shared>
extra_scripts = pre:buildroot/share/PlatformIO/scripts/common-cxxflags.py
build_flags = -fmax-errors=5 -g -D__MARLIN_FIRMWARE__ -fmerge-all-constants
# Sermoon D1 dış kütüphane gerektirmez (RTS DWIN raw UART kullanır,
# TMC2208 standalone modda, MAX31865/Sailfish/L6470 yok).
lib_deps =

[env]
framework   = arduino
build_flags = ${common.build_flags}
lib_deps    = ${common.lib_deps}

#
# Creality V4.3.1 (STM32F103RET6) — Sermoon D1
#
[env:creality]
platform          = ststm32@<6.2.0
board             = genericSTM32F103RE
board_build.core  = maple
build_flags       = !python Marlin/src/HAL/HAL_STM32F1/build_flags.py
  ${common.build_flags} -std=gnu++14 -DSTM32_XL_DENSITY
build_unflags     = -std=gnu++11
build_src_filter  = ${common.default_build_src_filter} +<src/HAL/HAL_STM32F1>
; NOT: extra_scripts [env:creality]'de tanimlaninca [common]'daki ayni isimli
; anahtari EZER (hicbir yer ${common.extra_scripts} interpolate etmiyor).
; Bu yuzden common-cxxflags.py BURADA acikca listelenmeli — aksi halde
; hic calismaz ve C++ flag'leri (-fno-exceptions/-fno-rtti) uygulanmaz.
lib_ignore        = Adafruit NeoPixel, SPI
; Configuration.h'daki BAUDRATE ile AYNI olmali (115200).
; Host portu (SERIAL_PORT 1) ve DWIN ekran portu (SERIAL_PORT_2 3) ayni
; BAUDRATE'i paylasir; DWIN T5L 115200 istedigi icin host da 115200'de.
; Yukseltmek icin once Configuration.h'a ayri bir BAUDRATE_2 eklenmeli.
monitor_speed     = 115200
extra_scripts     = pre:buildroot/share/PlatformIO/scripts/common-cxxflags.py
  buildroot/share/PlatformIO/scripts/creality.py
debug_tool        = jlink
upload_protocol   = serial
upload_port       = COM8
